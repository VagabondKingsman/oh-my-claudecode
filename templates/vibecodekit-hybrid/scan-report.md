# SCAN Report

> Generated by: `vibecodekit-hybrid-scan` skill
> Timestamp: {{TIMESTAMP}}
> Target path: {{REPO_PATH}}

## 1. Project Class
- [ ] Greenfield (empty or near-empty repo)
- [ ] Brownfield (existing code + patterns + tests)

## 2. Tech Stack
| Layer | Detected | Source of evidence |
|-------|----------|--------------------|
| Language(s) | | `package.json`, `pyproject.toml`, `go.mod`, `Cargo.toml`, … |
| Frontend framework | | — |
| Backend framework | | — |
| Datastore(s) | | — |
| Package manager | | — |
| Test runner | | — |
| Lint / format | | — |
| CI | | `.github/workflows/*`, `.gitlab-ci.yml`, … |
| Deployment | | — |

## 3. Modules / Top-level Surfaces
List the top-level directories / packages with a one-line purpose each.

| Module | Purpose | Notable files |
|--------|---------|---------------|
| | | |

## 4. Patterns Already in Use
- Architectural patterns (e.g. layered, hexagonal, MVC, feature-sliced)
- Naming conventions
- State management / error handling conventions
- Domain-specific idioms (e.g. OMC skill frontmatter, Vietnamese locale files)

## 5. Gaps / Smells
- Missing tests in …
- Inconsistent conventions between … and …
- Obvious tech-debt hotspots
- Security / auth concerns spotted during scan

## 6. Health Signals
| Signal | Value | Notes |
|--------|-------|-------|
| Last commit | | |
| Build status (if CI detected) | | |
| Test coverage (if reported) | | |
| Open issues / PRs (if visible) | | |

## 7. Size
- Files scanned: N
- Source LOC (approx.): N
- Largest files (> 800 LOC): …

## 8. Locale
| Field | Value |
|-------|-------|
| Detected locale | `en` \| `vi` |
| Winning signal | `omc-locale-json` \| `env:OMC_LOCALE` \| `flag:--locale` \| `readme-heuristic` \| `manifest-heuristic` \| `default` |
| Evidence snippet | *(≤ 200 chars of text that triggered the heuristic, or "n/a" for explicit signals)* |
| Non-default locale echoed to orchestrator? | yes \| no |

> Phase 3: if the winning signal is `readme-heuristic` or `manifest-heuristic`, include the ratio or match string so a reviewer can audit the heuristic without re-running it.

## 9. Ready-to-answer questions
Questions that the scan already answers (so RRI must NOT ask them again):
- …

## 10. Still-open questions (handoff to RRI)
Questions the human still has to answer (personas / priorities / scope):
- …

---
**Next step**: Hand this report to `vibecodekit-hybrid-rri` so the interview is scoped and does not re-ask auto-answered questions.
